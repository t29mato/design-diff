[project]
name = "design-diff"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "t29mato", email = "tomoya.matou@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "py2puml>=0.11.0",
]

[project.scripts]
design-diff = "design_diff:main"

[build-system]
requires = ["uv_build>=0.12.3,<0.13.0"]
build-backend = "uv_build"
